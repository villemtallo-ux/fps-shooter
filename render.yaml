services:
  - type: web
    name: neon-siege-coop
    runtime: node
    plan: free
    buildCommand: echo "No build needed"
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
